
import { render } from '@testing-library/react';
import { AppContent } from "./AppContent";

describe('AppContent', () => {
  it('should render', () => {

    render(
      <AppContent/>
    );
  })
});